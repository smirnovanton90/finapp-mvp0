## CodecProVariable

Положите сюда файл шрифта, например:

- `CodecProVariable.woff2`

После этого шрифт `CodecProVariable` будет использоваться глобально во всём веб-приложении (см. `app/globals.css`).

